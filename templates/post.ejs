<% posts.forEach(function(post){ %>
    <div class="post">
    <header>
        <h1><%= post.title %></h1> by <address><a href="#"><%= post.user %></a></address> posted on <time datetime="<%= post.date %></time>
    </header>
    <p><%= post.body %></p>
    </div>
<% }); %>